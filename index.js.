      } catch (err) {
        logEvent('ERROR', `Signup failure: ${err.message || String(err)}`);
        res.writeHead(500, { 'Content-Type': 'application/json' });
        res.end(JSON.stringify({ status: 'error', message: err.message || String(err) }));
      }
